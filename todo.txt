add a /tables post endpoint to create tables
create table domain
create player domain
when table created , user that created is first player added
then a table id is given and provided which is used by registered users to enter specific tables

add a /tables/{table-id}/join GET to create players
add a /tables/contributions POST create contributions